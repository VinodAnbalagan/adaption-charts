# Copy to .env (never commit .env). Both scripts auto-load this from repo root.

# Hugging Face write token: https://huggingface.co/settings/tokens
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx

# Adaption API key (for upload_to_adaption.py SDK path)
ADAPTION_API_KEY=pt_live_xxxxxxxxxxxxxxxxxxxx
